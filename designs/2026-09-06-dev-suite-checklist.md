# Dev Suite checklist — memory for designs/2026-09-06-dev-suite-loop.md
BASELINE_TESTS: 76  (C1 on 2026-09-07, unmodified tree: exit 0, "Executed 76 tests, with 3 tests skipped and 0 failures")
EXECUTION ORDER: phases 1 → 2 → 3 → 0 → 4 → 5 → 6 (gateway offline on 2026-09-07; HOST unknown; mutating probes approved by the user in chat 2026-09-07)
Legend: [ ] todo · [x] done (evidence line required) · [H] human checkpoint (never ticked by agent, never gating)
Item format after completion: `- [x] Pk.n … — RED: yes (<why>) — GREEN: C2 exit 0 <class> Executed <n>`

## Phase 0 — Live capture (no screenshot; needs HOST from human)
- [ ] P0.0 GATE: `test -f tools/.phase0-device.json` AND HOST known (from the ralph prompt line) AND `node tools/rpc-probe.mjs $HOST agents.list '{}'` returns JSON. If any is false → STOP-AND-ASK (cancel-ralph). Human approval for the mutating probes (P0.4–P0.6) was given in chat on 2026-09-07 — note it here.
- [ ] P0.1 `mkdir -p OpenClawMobile/Tests/Fixtures`; `node tools/rpc-probe.mjs $HOST sessions.list '{"includeDerivedTitles":true,"includeLastMessage":true}' > Tests/Fixtures/sessions.list.json` — CHECK: C5 valid non-empty JSON, no `^ERROR`; `grep -c '"status"' Tests/Fixtures/sessions.list.json` ≥ 1 (0 → §7 STOP-AND-ASK "rows lack status").
- [ ] P0.2 `tasks.list {}` → `Tests/Fixtures/tasks.list.json` — CHECK: C5.
- [ ] P0.3 `DUMP_HELLO=1 node tools/rpc-probe.mjs $HOST tasks.list '{}'` → `Tests/Fixtures/hello-ok.json` — CHECK: valid JSON; `grep -c attachments Tests/Fixtures/hello-ok.json` recorded (0 ⇒ defaults per ASSUMPTIONS).
- [ ] P0.4 (mutating — approved in P0.0) `sessions.create {"agentId":"main","label":"phase0-probe-2026-09-06"}` → `Tests/Fixtures/sessions.create.json`; note the returned session `key` and `sessionId` — CHECK: C5; any `forbidden`/scope error → §7 STOP-AND-ASK.
- [ ] P0.5 (mutating — approved) `chat.send` to the P0.4 key with message "phase0 attachment probe" + one ≤1 KB PNG attachment `{type:"image",mimeType:"image/png",fileName:"dot.png",content:<base64>,sizeBytes,width:1,height:1}` → `Tests/Fixtures/chat.send.attachment.ack.json`; then `chat.history {"sessionKey":<key>,"agentId":"main","limit":3}` → `Tests/Fixtures/chat.history.attachment.json`; then `chat.abort {"sessionKey":<key>,"agentId":"main"}` → `Tests/Fixtures/chat.abort.json` — CHECK: C5 on each; ack `ERROR` mentioning attachments → §7 STOP-AND-ASK.
- [ ] P0.6 (mutating — approved) with `--listen 5000` running: `sessions.patch {key,expectedSessionId,archived:true}` → `sessions.patch.archive.json`; `sessions.patch {…archived:false}` → `sessions.patch.unarchive.json`; `sessions.patch {…archived:true}` (leave archived); note whether a `sessions.changed` event arrived — CHECK: C5 each; scope error → STOP-AND-ASK; no `sessions.changed` observed → STOP-AND-ASK.
- [ ] P0.7 `Tests/Fixtures/README.md`: one line per method (`sessions.list`, `tasks.list`, `hello-ok`, `sessions.create`, `chat.send`, `chat.history`, `chat.abort`, `sessions.patch`) → `captured | unknown method | forbidden`, plus gateway version if hello-ok carries it — CHECK: `grep -cE '^\| (sessions\.list|tasks\.list|hello-ok|sessions\.create|chat\.send|chat\.history|chat\.abort|sessions\.patch)' OpenClawMobile/Tests/Fixtures/README.md` == 8.
- [ ] P0.8 GATE: C1 exit 0, `Executed N` ≥ BASELINE_TESTS (no code changed except tools/ flags). Record `PHASE 0 GATE`.

## Phase 1 — Retheme (screenshots: agents, chat, settings, create, profile)
- [x] P1.1 — RED: yes (compile errors: Theme has no member brand/bg/radiusCard/teal/warn/danger/textMuted) — GREEN: C2 exit 0 DesignSystemTests Executed 5 tests, 0 failures (0.02 s); old tokens renamed bgPrimary→bg, bgSecondary→card, textPrimary→text, textSecondary→textMuted across 7 files — `Tests/DesignSystemTests.swift::testTokenValues` — accent (1.000,0.361,0.361), brand (0.847,0.290,0.192), bg (0.055,0.063,0.082) ±1/255 via `UIColor(Theme.x).getRed`; `Theme.radius==6`, `Theme.radiusCard==10`, `Theme.border==1`. Implement tokens table §3.1 in Theme.swift (rename old tokens, keep `Color(hex:)`). — CHECK: C2 DesignSystemTests.
- [x] P1.2 — RED: yes (scratch `Features/ScratchViolation.swift` with `.shadow(`, `Color(hex:`, `cornerRadius: 8` → C2 exit 65, 3 failures naming ScratchViolation.swift:5/6/7; deleted) — GREEN: C2 exit 0 DesignSystemTests Executed 5 tests, 0 failures; C3 prints nothing — `DesignSystemTests::testNoShadows/testHexOnlyInTheme/testCornerRadiusLiteralOnlyInTheme` reading `Sources/` via `#filePath` — CHECK: C2 DesignSystemTests + C3 prints nothing. RED requirement: prove each test can fail by temporarily adding a violation in a scratch file, observing red, then deleting it (record).
- [x] P1.3 — RED: yes (AgentStatus has no member symbol; Theme.teal/warn/danger missing) — GREEN: C2 exit 0 DesignSystemTests::testStatusColorsAndSymbols passed; StatusBadge now shows the symbol — Status mapping in DesignSystem: running→teal, waiting/blocked/needsYou→warn, failed→danger, done/idle→textMuted; each carries an SF Symbol name — `DesignSystemTests::testStatusColorsAndSymbols` — CHECK: C2 DesignSystemTests.
- [x] P1.4 — no unit oracle by design; C1 exit 0 "Executed 81 tests, 3 skipped, 0 failures" after restyle; C3/C4 print nothing; mono now only in Theme.Font defs + MonoField input/code blocks/ids via Theme.Font.mono/monoCaption; PrimaryButton brand fill + white, secondary/destructive outlines; MessageBubble + ChatInputBar rewritten; labels sentence case — Restyle existing screens with new tokens: system type for UI, mono only for code/paths/ids; `PrimaryButton` fill=brand/white text; secondary=accent outline; destructive=danger outline; labels sentence case; radii via `Theme.radius`/`Theme.radiusCard`; no behavior change — CHECK: C1 exit 0 + C3/C4 empty.
- [x] P1.5 — CHECK: `grep -c FF5C5C CLAUDE.md` = 1, `grep -c '4pt everywhere' CLAUDE.md` = 0; rules now name palette, 6/10 radii, status symbols, type roles, and the DesignSystemTests gate — CLAUDE.md "Design system rules" updated: 6 pt controls / 10 pt cards, accent `#FF5C5C`, brand `#D84A31` — CHECK: `grep -n 'FF5C5C' CLAUDE.md` ≥ 1 AND `grep -c '4pt everywhere' CLAUDE.md` == 0.
- [x] P1.6 — PHASE 1 GATE: C1 exit 0, Executed 81 tests (≥ 76) after the restyle; the only later phase-1 edit was the one-line `--open-settings` DEBUG hook in AgentsListView (it was documented in CLAUDE.md but missing), which compiled in the C6 build and is re-verified by the phase-2 gate C1. Screenshots read: `agents.png` (title "Agents", 3 rows with tinted emoji chips, amber demo banner, red + and tab tint), `chat.png` (Assistant header + "Idle", two full-width agent cards with emoji/name header, right-aligned tinted user bubble, composer), `settings.png` ("Pair this device", brand "Scan Setup Code", outlined "Pair Device", "Advanced: host, token, model"), `create.png` ("New Agent", sentence-case labels, brand "Create Agent"), `profile.png` ("Profile", Assistant heading, mono ID/Workspace/Model rows, "Instructions (AGENTS.md)"). — GATE: C1 exit 0, N ≥ BASELINE_TESTS. Screenshots via C6: `agents` with NO arg, `chat` with `--seed-demo`, `settings` with `--open-settings`, `create` with `--open-create`, `profile` with `--open-profile main`. READ each; record visible titles.
- [H] P1.H Human: review the 5 screenshots for OpenClaw look (red accent, blue-black bg, 6/10 radii, no green accent left). PENDING FOR HUMAN.

## Phase 2 — Chat core (screenshot: chat re-taken showing a code block)
- [x] P2.1 — RED: yes (cannot find 'ChatThread'; InboundEnvelope has no member 'matchesSession') — GREEN: C2 exit 0 ChatThreadTests Executed 3 tests, 0 failures; `Models/ChatThread.swift` + `InboundEnvelope.matchesSession` (sessionKey first, agentId-only frames → that agent's main thread only) — `Tests/ChatThreadTests.swift`: `ChatThread{sessionKey,agentId,title,emoji}`; `InboundEnvelope.matchesSession("agent:x:task1")` true for payload.sessionKey match, false for other key, and main-thread fallback (no sessionKey in payload, agentId matches, key `agent:x:main`) — frames derived from `MockGateway`'s live-shaped `session.message` shape — CHECK: C2 ChatThreadTests.
- [x] P2.2 — CHECK: `grep -rn 'as? GatewayWSSyncSource' Sources/Features` prints nothing; C1 exit 0 Executed 84 tests (MockGatewayE2ETests green via the agentId conveniences in `extension SyncSource`). `SyncSource` now: `loadHistory(sessionKey:agentId:)`, `subscribe(sessionKey:)`, `activityStream(sessionKey:)`, `send(sessionKey:agentId:text:idempotencyKey:) -> runId?`, `abort(sessionKey:agentId:runId:)`; `MainAgentTask.run` takes `any SyncSource`; Create/Profile VMs lost their casts. Deviation: the `attachments:` parameter arrives with the `Attachment` type in P3.2 (default `[]`) rather than as a dead parameter now — `send(sessionKey:agentId:text:idempotencyKey:attachments:)` + `abort(sessionKey:agentId:runId:)` on `SyncSource`; `DemoSyncSource` conforms (canned) — CHECK: `grep -rn 'as? GatewayWSSyncSource' OpenClawMobile/Sources/Features` prints nothing AND C1 compiles (existing MockGatewayE2ETests still green).
- [x] P2.3 — RED: yes (cannot find 'MessageSegmenter'); while red, expectations were refined to the rule "fence lines own their line terminators, prose body is newline-trimmed, raw round-trips" — GREEN: C2 exit 0 MessageSegmenterTests Executed 7 tests, 0 failures (`Features/Chat/MessageSegmenter.swift`) — `Tests/MessageSegmenterTests.swift`: prose-only; single fence; fence with language; two fences; unclosed fence (streaming) → expected `[Segment]`; `segments.map(text).joined() == input` for all — CHECK: C2 MessageSegmenterTests.
- [x] P2.4 — RED: yes (cannot find 'ToolEvent'/'ToolTimeline') — GREEN: C2 exit 0 ToolEventTests Executed 8 tests, 0 failures; AgentActivityTests (14) + WireProtocolTests (10) still green after `EventData.args` lenient string-only decode (`// ponytail:` noted) — `Features/Chat/ToolTimeline.swift` — `Tests/ToolEventTests.swift`: `ToolEvent.from(env)` on the verbatim WebSearch/Bash frames from AgentActivityTests → `summary == "x"` / `"ls"`; frame without args → `summary == nil`; `phase:"result"` closes the open entry; summary truncated to 80 chars. Requires string-valued `EventData.args` decode (see ASSUMPTIONS) — CHECK: C2 ToolEventTests + C2 AgentActivityTests (unchanged, still green).
- [x] P2.5 — RED: yes (MockGateway has no `holdFinal`/`receivedFrames`; ChatViewModel has no `canStop`/`stop`) — GREEN: C2 exit 0 ChatStopRetryTests::testStopSendsAbortAndMarksAborted passed (0.57 s): abort frame `sessionKey == agent:main:main`, bubble aborted && !failed && !isStreaming; `activeRunId` armed by the send ack, cleared by the run's final/aborted or activity `.idle`; Stop button in the composer — Stop: MockGateway handles `chat.abort` (records frame, emits `chat state:"aborted"`); `Tests/ChatStopRetryTests.swift::testStopSendsAbortAndMarksAborted` — while streaming, `vm.stop()` → `gateway.receivedMethods.contains("chat.abort")`, frame `sessionKey == thread.sessionKey`, bubble `.aborted` not `.failed` — CHECK: C2 ChatStopRetryTests.
- [x] P2.6 — RED: yes (ChatViewModel has no `retry`) — GREEN: C2 exit 0 ChatStopRetryTests::testRetryUsesNewIdempotencyKey passed: two distinct keys recorded by the flaky seam, failed flag cleared, bubble carries the new key; "Failed to send. Retry" button on the bubble — Retry: `ChatStopRetryTests::testRetryUsesNewIdempotencyKey` — sync stub throws once; `vm.retry(message)` → second `chat.send` with a different idempotencyKey; failed flag cleared — CHECK: C2 ChatStopRetryTests.
- [x] P2.7 — no unit oracle by design; C1 exit 0 after adding `Features/Chat/CodeBlockView.swift` (language label, mono, horizontal scroll, Copy→"Copied"), MessageBubble renders `MessageSegmenter` segments (prose via inline-only `AttributedString(markdown:)`), context menu Copy text · Share (ShareLink) · Copy code per block; C3 prints nothing; the demo canned reply already carries a ``` fence — `CodeBlockView` (language label, mono, horizontal scroll, Copy) + message context menu (Copy text · Share · Copy code); prose via `Text(AttributedString(markdown:))` inline-only. Demo canned reply gains one fenced block — CHECK: C1 exit 0 + C3 empty.
- [x] P2.8 — C1 exit 0; `grep -rn 'ToolEvent.from' Sources` = 1 hit in `Services/GatewayWSSyncSource.swift:183` (the mapping lives in the seam's new `toolEvents(sessionKey:)` so every conformer can supply tool events; the checklist's original path `Sources/Features/Chat` only held the type — scope widened to `Sources/`, intent unchanged: entries come from `ToolEvent.from` on real frames). `ChatViewModel.timeline` resets per send, closes on activity `.idle`/Stop; `ToolTimelineView` collapsed row → expandable list, unknown tool names verbatim — Tool timeline view in thread (entries from `ToolEvent`, closed on result/final/lifecycle end); unknown tool → name shown verbatim, no invented verb — CHECK: C1 exit 0; `grep -rn 'ToolEvent.from' OpenClawMobile/Sources/Features/Chat` ≥ 1.
- [x] P2.9 — PHASE 2 GATE: C1 exit 0, Executed 101 tests (3 skipped, 0 failures) after the last phase-2 edit; `chat.png` re-taken and read: Assistant card shows inline `feat/auth` in mono and a code block ("code" label, mono body, red Copy button) followed by prose; composer + send button visible — GATE: C1 exit 0, N ≥ BASELINE_TESTS. Re-take `chat` via `--seed-demo` (canned fenced reply visible). READ it: code block with Copy visible.

## Phase 3 — Attachments + STT (screenshot: chat re-taken showing attach + mic buttons)
- [x] P3.1 — RED: yes (cannot find 'AttachmentPolicy') — GREEN: C2 exit 0 AttachmentBudgetTests Executed 8 tests, 0 failures (`Features/Chat/Attachments.swift`: AttachmentPolicy + Attachment + AttachmentBudget); hello-ok fixture absent → `.default` case exercised; RE-RUN the fixture case after P0.3 — `Tests/AttachmentBudgetTests.swift`: 7 MB image → plan includes the re-encode step; 8 KB `.swift` → `.inlineFence`; 21 MB PDF → `.tooLarge(max:)`; encoded request > `maxPayload` → `.payloadTooLarge` before send; `AttachmentPolicy` decodes from `Tests/Fixtures/hello-ok.json` `policy.attachments` when that file exists, else equals `.default` (phase 3 runs before phase 0, so the decode sub-case is re-run and recorded after P0.3 lands) — CHECK: C2 AttachmentBudgetTests.
- [x] P3.2 — RED: yes (ChatViewModel has no `pendingAttachments`; ChatMessage has no `attachments`) — GREEN: C2 exit 0 AttachmentSendTests Executed 3 tests, 0 failures (one bubble with 2 attachments after echo; frame `attachments` = 2 × {type,mimeType,fileName,content,sizeBytes,width}; attachment-only send allowed; 20 MiB frame fails locally with no `chat.send`); ChatStopRetryTests still 2/2 after the seam gained `attachments:`; `AttachmentStrip` renders thumbnails/chips. ponytail: policy = protocol defaults until a probe shows different limits — `Tests/AttachmentSendTests.swift`: two attachments → ONE optimistic bubble with two thumbnails; MockGateway `chat.send` echoes `idempotencyKey` → no duplicate bubble; frame `attachments.count == 2` with `type,mimeType,fileName,content,sizeBytes` — CHECK: C2 AttachmentSendTests.
- [x] P3.3 — RED: yes (cannot find 'DictationState') — GREEN: C2 exit 0 SpeechDictationTests Executed 7 tests, 0 failures (`Features/Chat/SpeechDictation.swift` pure reducer: idle→requesting→listening→finishing→idle | denied | failed; denied leaves draft untouched; partials replace only the dictated span) — `Tests/SpeechDictationTests.swift`: reducer `idle→requesting→listening→finishing→idle|denied|failed`; `.denied` from `.requesting` leaves `draft` unchanged; partial result replaces only the dictated span (typed prefix preserved) — CHECK: C2 SpeechDictationTests.
- [x] P3.4 — CHECK: `grep -c` of the three usage keys in project.yml = 3 (camera string also updated for photos), C0 regenerated, C1 exit 0 Executed 119 tests. PhotosPicker (`.photosPicker`), Camera (`CameraPicker` over UIImagePickerController, hidden when unavailable), Files (`.fileImporter` `.item`, multiple); `ImageReencoder.jpeg` one pass 2048 px / 0.8 then `verifyImage`; text files ≤ 64 KB inline as a fence into the draft; `SpeechDictation` runtime (SFSpeechRecognizer + AVAudioEngine, on-device when supported) drives the reducer — Pickers wired (PhotosPicker, camera via UIImagePickerController, files via `.fileImporter` `UTType.item`); one JPEG re-encode at 2048 px / 0.8; Info.plist strings `NSPhotoLibraryUsageDescription`, `NSMicrophoneUsageDescription`, `NSSpeechRecognitionUsageDescription`, updated `NSCameraUsageDescription` — CHECK: `grep -c 'NSPhotoLibraryUsageDescription\|NSMicrophoneUsageDescription\|NSSpeechRecognitionUsageDescription' OpenClawMobile/project.yml` == 3, C0, C1 exit 0.
- [x] P3.5 — CHECK: `grep -rn 'Enable microphone' Sources` = 1 (`SpeechDictation.deniedHint`, shown in the composer hint line; P3.3 denied case green) — Denied mic/speech → inline hint "Enable microphone & speech in Settings", no crash — CHECK: `grep -rn 'Enable microphone' OpenClawMobile/Sources` ≥ 1 + P3.3 denied case green.
- [x] P3.6 — PHASE 3 GATE: C1 exit 0, Executed 119 tests (3 skipped, 0 failures) after the last phase-3 edit; C3/C4 print nothing; `chat.png` re-taken and read: composer shows red "+" attach button, "Message…" field, mic button, send button; code block with Copy still rendered above. NEXT in execution order is phase 0, which needs HOST → STOP-AND-ASK recorded under QUESTIONS; loop cancelled via cancel-ralph on 2026-09-07 — GATE: C1 exit 0, N ≥ BASELINE_TESTS. Re-take `chat` screenshot (`--seed-demo`): composer shows attach + mic controls. READ it.
- [H] P3.H Human (simulator): photo-library pick renders thumbnail chip and sends. PENDING FOR HUMAN.
- [H] P3.H2 Human (device only): camera capture and microphone dictation produce an attachment / draft text. PENDING FOR HUMAN.

## Phase 4 — Board read-only (screenshot: board)
- [ ] P4.1 `Tests/BoardModelTests.swift::testDecodesLiveFixtures`: `SessionSummary` from `Fixtures/sessions.list.json`, `TaskSummary` from `Fixtures/tasks.list.json`; both non-empty else fail — CHECK: C2 BoardModelTests.
- [ ] P4.2 `BoardModelTests::testColumnRuleTable`: 8 rules (§5.2) one row each + precedence archived+running→done, running+unread→running, failed+unread→needsYou; rows = decoded live row with fields mutated in Swift — CHECK: C2 BoardModelTests.
- [ ] P4.3 `BoardModelTests::testLaneRulePrecedence`: category > worktree.repoRoot > execCwd ?? spawnedCwd > agent workspace > agent name — CHECK: C2 BoardModelTests.
- [ ] P4.4 `BoardModelTests::testFoldRule`: child with `spawnedBy` == a board card's key appears as that card's sub-task, not its own card; orphan shows own card — CHECK: C2 BoardModelTests.
- [ ] P4.5 `BoardModelTests::testOrderingAndHiding`: lanes and cards sort by `lastActivityAt` desc; `isMain` and `kind ∈ {global,unknown}` hidden — CHECK: C2 BoardModelTests.
- [ ] P4.6 `SyncSource.listSessions/listTasks/sessionChanges`; MockGateway serves fixtures; `Tests/BoardViewModelTests.swift::testLoadsFromGateway` — non-empty lanes — CHECK: C2 BoardViewModelTests.
- [ ] P4.7 `BoardViewModelTests::testSessionsChangedRefreshes`: MockGateway emits `sessions.changed` with a changed `status` → card column changes without manual refresh — CHECK: C2 BoardViewModelTests.
- [ ] P4.8 `BoardViewModelTests::testDemoRendersThreeLanes`: `DemoSyncSource` → 3 lanes, ~6 sessions, some tasks — CHECK: C2 BoardViewModelTests.
- [ ] P4.9 Board tab in `RootTabView` (columns as `TabView(.page)`, lanes collapsible), `--open-board` QA arg — CHECK: `grep -n 'open-board' OpenClawMobile/Sources/Features/Root/RootTabView.swift OpenClawMobile/Sources/Features/Board/*.swift` ≥ 1; C1 exit 0.
- [ ] P4.10 GATE: C1 exit 0, N ≥ BASELINE_TESTS. Screenshot `board` via `--open-board` (demo). READ it: three lane headers visible.

## Phase 5 — Board interactive (screenshot: board re-taken)
- [ ] P5.1 MockGateway: `receivedFrames` (full params), handlers `sessions.patch`/`sessions.create`/`tasks.cancel`, `failNextPatch`, emits `sessions.changed` after successful patch — CHECK: C1 still green (no behavior change yet).
- [ ] P5.2 `Tests/BoardInteractionTests.swift::testArchiveSendsPatchWithExpectedSessionId` — CHECK: C2 BoardInteractionTests.
- [ ] P5.3 `::testUnarchiveSendsArchivedFalse` — CHECK: C2.
- [ ] P5.4 `::testLaneMoveSendsCategory` — `sessions.patch{category}` only — CHECK: C2.
- [ ] P5.5 `::testStartSendsChatSendToCardKey` — Backlog→Running sends `chat.send{sessionKey: card.key, message: title}` — CHECK: C2.
- [ ] P5.6 `::testCancelSendsTasksCancel` — `tasks.cancel{taskId}` — CHECK: C2.
- [ ] P5.7 `::testStopRunSendsChatAbort` — `chat.abort{sessionKey: card.key}` — CHECK: C2.
- [ ] P5.8 `::testFailingPatchRestoresState` — `failNextPatch=true` → board equals pre-action snapshot, error toast set — CHECK: C2.
- [ ] P5.9 `::testNewTaskSendsSessionsCreate` — `{agentId,label,category}` — CHECK: C2.
- [ ] P5.10 Archive-running confirm sheet copy states the gateway cancels active work — CHECK: `grep -rn 'cancel' OpenClawMobile/Sources/Features/Board/BoardView.swift` ≥ 1 within the confirm dialog text.
- [ ] P5.11 GATE: C1 exit 0, N ≥ BASELINE_TESTS. Re-take `board` screenshot. READ it.
- [H] P5.H Human (simulator): drag card → Done chip; drag → lane header; context menu actions; confirm sheet wording. PENDING FOR HUMAN.

## Phase 6 — Docs
- [ ] P6.1 `.docs/architecture.md` §8/§9 + CLAUDE.md layout tree updated for `Features/Board` and the new `Features/Chat` files — CHECK: `grep -n 'Features/Board\|Board/' CLAUDE.md` ≥ 1.
- [ ] P6.2 FINAL: C1 exit 0, N ≥ BASELINE_TESTS; C3 + C4 empty; all 6 screenshots present (`ls designs/assets/2026-09-06-dev-suite-*.png | wc -l` ≥ 6). Summarize with **PENDING FOR HUMAN** listing P1.H, P3.H, P3.H2, P5.H verbatim. Only then output: DEV SUITE LOOP COMPLETE

## QUESTIONS
- 2026-09-07 (after the phase-3 gate): Phases 1–3 are green. Phase 0 needs the gateway. **What is the gateway host (wss://… or https://…) for phase-0 fixture capture?** Restart with:
  `/ralph-loop "Read designs/2026-09-06-dev-suite-loop.md and keep executing unchecked checklist items. HOST=<url>. Mutating phase-0 probes approved 2026-09-07. Stop only when the completion promise is literally true" --max-iterations 60 --completion-promise "DEV SUITE LOOP COMPLETE"`

## BLOCKED
(none yet)
